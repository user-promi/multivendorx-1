import { useEffect, useState } from 'react';
import axios from 'axios';
import { BasicInput, SelectInput, getApiLink, SuccessNotice } from 'zyra';

const StoreReview = ({ id, data }: { id: string|null; data: any }) => {
}

export default StoreReview;