import { useEffect, useState } from 'react';
import axios from 'axios';
import { BasicInput, SelectInput, getApiLink, SuccessNotice } from 'zyra';

const StoreRefund = ({ id, data }: { id: string|null; data: any }) => {
}

export default StoreRefund;